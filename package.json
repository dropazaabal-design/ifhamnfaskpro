{
  "name": "matjar-pro",
  "version": "0.1.0",
  "private": true,
  "description": "قالب ووكومرس عربي RTL أصلي — بيئة البناء",
  "license": "GPL-2.0-or-later",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "fonts": "node tools/copy-fonts.mjs",
    "css:dev": "tailwindcss -i src/css/main.css -o assets/css/main.css --watch",
    "css:build": "tailwindcss -i src/css/main.css -o assets/css/main.css --minify",
    "js:dev": "esbuild src/js/main.js src/js/product.js src/js/checkout.js --bundle --format=iife --target=es2019 --sourcemap --outdir=assets/js --watch",
    "js:build": "esbuild src/js/main.js src/js/product.js src/js/checkout.js --bundle --minify --format=iife --target=es2019 --outdir=assets/js",
    "build": "npm run fonts && npm run css:build && npm run js:build",
    "dev": "npm run css:dev & npm run js:dev",
    "verify:contrast": "php tools/verify-contrast.php",
    "smoke": "php tools/render-smoke.php && php tools/render-smoke.php --woocommerce && php tools/render-smoke.php --woocommerce --overlay && php tools/render-smoke.php --card && php tools/render-smoke.php --funnel",
    "verify": "npm run verify:contrast && npm run smoke",
    "preview": "node tools/preview.mjs"
  },
  "devDependencies": {
    "@fontsource-variable/cairo": "^5.3.0",
    "@fontsource/ibm-plex-sans-arabic": "^5.3.0",
    "autoprefixer": "^10.4.20",
    "esbuild": "^0.28.0",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.19"
  }
}
